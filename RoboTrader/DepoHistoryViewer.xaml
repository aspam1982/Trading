<Window x:Class="RoboTrader.DepoHistoryViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RoboTrader"
        xmlns:wpf="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        mc:Ignorable="d"
        Title="История депозита" Height="450" Width="800" WindowState="Maximized" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <GroupBox Header="Таймфрэйм" Grid.Row="0">
                <ComboBox x:Name="timeframeselector" SelectionChanged="timeframeselector_SelectionChanged">
                    <ComboBoxItem Content="Минуты" Tag="0"/>
                    <ComboBoxItem Content="Часы" Tag="1" IsSelected="True"/>
                    <ComboBoxItem Content="Дни" Tag="2"/>
                </ComboBox>
            </GroupBox>
            <Grid Grid.Row="1" x:Name="plotcontainer"/>
        </Grid>
        <GridSplitter Grid.Column="1" ResizeDirection="Columns"/>
        <ScrollViewer Grid.Column="2" Width="300">
            <ListView x:Name="operations"/>
        </ScrollViewer>
    </Grid>
</Window>
