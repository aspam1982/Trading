<Window x:Class="RoboTrader.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RoboTrader"
                xmlns:xceed="http://schemas.xceed.com/wpf/xaml/toolkit"

        mc:Ignorable="d"
        Title="Настройки робота" Height="600" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <xceed:PropertyGrid SelectedObject="{Binding}" AutoGenerateProperties="True" ShowSearchBox="False" ShowSummary="False" ShowSortOptions="False" ShowTitle="False" IsCategorized="True"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
            <Button Content="Сохранить настройки" Margin="5" Padding="5" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
