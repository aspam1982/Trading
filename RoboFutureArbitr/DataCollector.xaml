<Window x:Class="RoboFutureArbitr.DataCollector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RoboFutureArbitr"
        mc:Ignorable="d"
        Title="DataCollector" Height="450" Width="800" Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ListView Grid.Column="0" ItemsSource="{Binding lastpriceslist}" DisplayMemberPath="Value"/>
            <ListView Grid.Column="1" ItemsSource="{Binding lastcandleslist}" DisplayMemberPath="Value"/>
            <ListView Grid.Column="2" ItemsSource="{Binding lastorderbookslist}" DisplayMemberPath="Value"/>
        </Grid>
        <StackPanel Grid.Row ="1" Orientation="Horizontal">
            <Button Content="PushMe" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
