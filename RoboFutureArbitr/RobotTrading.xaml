<Window x:Class="RoboFutureArbitr.RobotTrading"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xceed="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:RoboFutureArbitr"
        mc:Ignorable="d"
        Title="RobotTrading" Height="450" Width="800" WindowState="Maximized" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_Robot">
                <MenuItem Header="Clean console" Command="{Binding CleanConsoleCommand}"/>
                <Separator/>
                <MenuItem Header="Start" Command="{Binding StartCommand}"/>
                <MenuItem Header="Stop" Command="{Binding StopCommand}"/>
            </MenuItem>
            <MenuItem Header="Statistics" Command="{Binding StatisticsCommand}"/>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ListView ItemsSource="{Binding Robot.FutureStatistics }"  Grid.Column="0" MinWidth="200">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <xceed:RichTextBox IsReadOnly="True" IsHitTestVisible="False" IsReadOnlyCaretVisible="False" Margin="3" Text="{Binding DisplayInfo, Mode=OneWay}" Background="Transparent" BorderThickness="0" Width="500">
                            <xceed:RichTextBox.TextFormatter>
                                <local:CustomTextFormatter/>
                            </xceed:RichTextBox.TextFormatter>
                        </xceed:RichTextBox>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListView>
            <TextBox Text="{Binding ConsoleText}" Grid.Column="1" IsReadOnly="True" TextChanged="TextBox_TextChanged" />
        </Grid>
    </Grid>
</Window>
